import { DdToggleDirective } from './dd-toggle.directive';

describe('DdToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new DdToggleDirective();
    expect(directive).toBeTruthy();
  });
});
